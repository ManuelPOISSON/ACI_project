version: '3'
networks:
  pulsar:
      name: test_pulsar
      external: true

services:
  client:
    image: apachepulsar/pulsar-test-latest-version:latest
    volumes:
      - .:/workspace:cached
    networks:
      pulsar: